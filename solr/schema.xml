<?xml version="1.0" encoding="UTF-8" ?>

<schema name="audioaddict-echoprint" version="1.5">

  <fieldType name="int" class="solr.TrieIntField" precisionStep="0" omitNorms="true" positionIncrementGap="0"/>
  <fieldType name="date" class="solr.TrieDateField" precisionStep="0" positionIncrementGap="0"/>
  <fieldType name="string" class="solr.StrField" sortMissingLast="true" multiValued="false" />
  <fieldType name="long" class="solr.TrieLongField" precisionStep="0" positionIncrementGap="0" multiValued="false"/>

  <!-- Solr internal unique ID. These do not correspond to AudioAddict tracks.id values -->
  <field name="id" type="string" indexed="true" stored="true" required="true" multiValued="false"/>

  <!-- Solr internal use field for locking, etc -->
  <field name="_version_" type="long" indexed="true" stored="true"/>

  <!-- For Testing purposes -->
  <field name="filename" type="string" indexed="true" stored="true" required="true" multiValued="false"/>

  <!-- The expanded codes for a track, and the corresponding time values. -->
  <!-- We can split these pairs up because multivalued fields are order-safe in lucene -->
  <field name="codes" type="int" indexed="true" stored="true" required="true" multiValued="true"/>
  <field name="times" type="int" indexed="true" stored="true" required="true" multiValued="true"/>
  <field name="version" type="string" indexed="true" stored="true" required="true" multiValued="false"/>


  <!-- AudioAddict content fields -->
  <field name="trackId" type="int" indexed="true" stored="true" required="true" multiValued="false"/>
  <field name="upc" type="string" indexed="true" stored="true" required="false" multiValued="false"/>
  <field name="isrc" type="string" indexed="true" stored="true" required="false" multiValued="false"/>

  <field name="ingestedAt" type="date" indexed="true" stored="true" required="true" multiValued="false" default="NOW"/>

  <!-- Field to use to determine and enforce document uniqueness.
       Unless this field is marked with required="false", it will be a required field
  -->
  <uniqueKey>trackId</uniqueKey>

  <!-- field for the QueryParser to use when an explicit fieldname is absent -->
  <defaultSearchField>codes</defaultSearchField>

  <!-- SolrQueryParser configuration: defaultOperator="AND|OR" -->
  <solrQueryParser defaultOperator="OR"/>

</schema>
